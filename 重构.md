# 重构.md — opencode2api_electron 搭建手册（目录结构驱动）

> 面向执行者（AI / 开发者）：**照着目标目录树，逐个文件落地**，即可一比一复刻当前项目（Tauri 版 opencode2api 管理器）的全部功能。
> 本文档每个节点 = 一个文件/目录，给出：职责、关键实现要点、依赖、验收标准。
> 核心算法（端口/密钥/扫描/启动链路）内嵌在对应文件节点，实现时直接照抄逻辑，无需翻别的文档。

---

## 〇、开工前：复制清单与新机器环境

### 0.1 从旧机器复制（文件系统复制，不受 .gitignore 影响）

| 源目录 | 作用 | 复制时排除 |
|---|---|---|
| `opencode2api/` | 当前项目源码 + 二进制 | `.git/`、`node_modules/`、`src-tauri/target/`；`dist/` 可省 |
| `opencode2api_enhance/` | Go 源码母本 + 旧增强版参考 | `.git/`、`src-tauri/target/`、`node_modules/`、`serve.out.log`/`serve.err.log` |

**关键二进制**（已含在 `opencode2api/bin/`，复制时务必带上，别被 gitignore 误导）：
- `opencode2api/bin/opencode2api.exe`（9.9MB，Go 代理）
- `opencode2api/bin/sing-box.exe`（43.3MB，代理内核）

### 0.2 新机器安装（不是复制）

| 工具 | 版本 | 用途 |
|---|---|---|
| Node.js | ≥ 18 | Electron 开发/构建必需 |
| Go | ≥ 1.22 | 重新编译 opencode2api.exe（**不需要门禁可跳过**，直接用 bin 里的旧 exe） |
| ~~Rust / MSVC~~ | — | **不再需要**（Electron 方案） |

### 0.3 开始前的两个决策点

1. **401 门禁要不要**：当前 `opencode2api/main.go` 已含 `apiKeyAuthMiddleware`（/v1/* 必须带 `Bearer <实例密钥>`，否则 401），**未编译**。要启用 → 新机器上 `go build` 重新出 exe；不启用 → 直接用 `bin/opencode2api.exe`。
2. **Go 代理重不重写**：本手册默认**保持 Go 二进制**（最稳）。若未来用 Node 重写，需覆盖 OpenAI/Anthropic 协议转换 + 上游路由 + 流式转发，见 §附录 B。

---

## 一、目标工程目录树（总览）

```
opencode2api_electron/
├── package.json                        # ① 依赖与脚本
├── electron-builder.yml                # ② 打包配置
├── tsconfig.json                       # ③ TS 工程配置
├── vite.config.ts                      # ④ 前端构建（含主进程/preload 用 esbuild）
├── electron/                           # ⑤ 主进程（全部后端逻辑）
│   ├── main.ts                         # ⑤.1 入口：userData/窗口/托盘/生命周期
│   ├── preload.ts                      # ⑤.2 contextBridge → window.api
│   ├── ipc.ts                          # ⑤.3 26 个 IPC handler
│   └── services/                       # ⑤.4 服务层（核心业务）
│       ├── clash.ts                    # 节点解析（外部控制 + Verge profiles + 分组/过滤）
│       ├── configStore.ts              # config.json 读写
│       ├── opencodeCfg.ts              # opencode2api 配置模板
│       ├── singboxCfg.ts               # sing-box 配置模板
│       ├── instanceManager.ts          # 实例 CRUD / 启停 / 探活
│       ├── scanner.ts                  # 节点扫描控制器
│       ├── binaries.ts                 # 内嵌二进制释放/存在性
│       └── http.ts                     # 裸 HTTP GET / 端口等待（TCP 工具）
├── src/                                # ⑥ 前端（从 opencode2api/src 复制后改 1 个文件）
│   ├── App.tsx
│   ├── main.tsx / index.css / vite-env.d.ts
│   ├── components/TitleBar.tsx         # ⑥.1 自绘标题栏（改拖拽实现）
│   ├── pages/                          # ⑥.2 InstancesPage / NodesPage / SettingsPage
│   └── lib/api.ts                      # ⑥.3 唯一需要改的前端文件：invoke → window.api
├── resources/
│   └── bin/                            # ⑦ 二进制（从 opencode2api/bin 复制）
│       ├── opencode2api.exe
│       └── sing-box.exe
└── README.txt                          # ⑧ 便携包使用说明（从 opencode2api/portable 复制）
```

---

## 二、逐节点实现手册

### ① `package.json` — 依赖与脚本

```jsonc
{
  "name": "opencode2api-manager",
  "version": "0.1.0",
  "main": "dist-electron/main.js",          // electron 入口（esbuild 产物）
  "scripts": {
    "dev": "electron-vite dev",              // 或自配 esbuild + electron .
    "build": "tsc -b && vite build",         // 渲染进程
    "build:electron": "esbuild electron/main.ts electron/preload.ts electron/ipc.ts ...",
    "dist": "npm run build && npm run build:electron && electron-builder"
  },
  "dependencies": {
    "js-yaml": "^4"                          // Clash YAML 解析（唯一新增运行时依赖）
  },
  "devDependencies": {
    "electron": "^30", "electron-builder": "^24",
    "electron-vite": "^2",                   // 可选脚手架，推荐
    "vite": "^8", "@vitejs/plugin-react": "^6", "typescript": "~6",
    "tailwindcss": "^4", "@tailwindcss/vite": "^4",
    "@types/react": "^19", "@types/react-dom": "^19", "@types/js-yaml": "^4",
    "@types/node": "^24"
  }
}
```

前端依赖沿用现项目：`react@^19`、`react-dom@^19`、`lucide-react@^1`、`clsx@^2`（见现项目 package.json）。
> Rust 侧依赖（serde/tokio/serde_yaml/anyhow/sysinfo/dirs）**全部不需要**。

### ② `electron-builder.yml` — 打包配置

```yaml
appId: com.opencode2api.manager
productName: opencode2api 管理器
directories: { output: dist }
files: ["dist/**", "dist-electron/**"]
extraResources:
  - from: resources/bin
    to: bin                     # 运行时二进制 → <安装目录>/resources/bin
win:
  target: [portable, nsis]      # 便携 + 安装版
  icon: resources/icon.ico      # 沿用现项目图标
```

### ③ `tsconfig.json`

按现项目 `tsconfig.json`（React + Vite 标准配置）扩展出 electron 侧的 `tsconfig.electron.json`（module: commonjs 或 node16，target: es2022，types: ["node"]）。

### ④ `vite.config.ts` — 构建

沿用现项目（React 插件 + Tailwind）。Electron 主进程/preload 用 esbuild 单独编译（`electron-vite` 已集成，可直接用其配置约定）。

---

### ⑤.1 `electron/main.ts` — 入口（窗口/托盘/生命周期）

**职责**：应用启动顺序、窗口创建、托盘、关闭隐藏、退出清理、userData 指向。

```ts
import { app, BrowserWindow, Tray, Menu, nativeImage, ipcMain } from 'electron'
import { ensureBinaries } from './services/binaries'
import { stopAllInstances } from './services/instanceManager'

let win: BrowserWindow | null = null
let tray: Tray | null = null
let isQuitting = false

// 1. userData 指向旧版同款目录（数据无缝迁移的关键！）
app.setPath('userData', require('path').join(app.getPath('appData'), 'opencode2api-manager'))

// 2. 单实例锁（可选增强）
if (!app.requestSingleInstanceLock()) { app.quit(); return }

app.whenReady().then(() => {
  ensureBinaries()                       // 释放/校验 bin/ 下两个 exe
  createWindow()
  createTray()
})

function createWindow() {
  win = new BrowserWindow({
    width: 980, height: 680, minWidth: 820, minHeight: 560, center: true,
    frame: false,                          // 无边框（对应 Tauri decorations:false）
    title: 'opencode2api 管理器',
    webPreferences: { preload: require('path').join(__dirname, 'preload.js'), contextIsolation: true },
  })
  win.loadURL(process.env.VITE_DEV_SERVER_URL || 'file://.../dist/index.html')
  win.on('close', (e) => {                 // 关闭 = 隐藏，实例继续运行
    if (!isQuitting) { e.preventDefault(); win?.hide() }
  })
  win.on('maximize', () => win?.webContents.send('win:maximize', true))
  win.on('unmaximize', () => win?.webContents.send('win:maximize', false))
}

function createTray() {
  tray = new Tray(nativeImage.createFromPath('resources/icon.ico'))
  tray.setToolTip('opencode2api 管理器')
  tray.setContextMenu(Menu.buildFromTemplate([
    { label: '显示主窗口', click: () => { win?.show(); win?.unmaximize() /* 若最小化需 restore */; win?.focus() } },
    { label: '退出', click: () => { isQuitting = true; app.quit() } },
  ]))
  tray.on('click', () => { win?.show(); win?.focus() })   // 左键单击显示
}

app.on('before-quit', async (e) => {        // 退出前停止全部实例（对应 stop_all_instances）
  if (!isQuitting) return
  e.preventDefault()
  await stopAllInstances()                  // 逐个 taskkill
  app.exit(0)
})
```

**验收**：关窗隐藏、托盘显示/退出、退出时 sing-box+opencode2api 全部被 taskkill、重启后实例列表还在。

---

### ⑤.2 `electron/preload.ts` — window.api

```ts
import { contextBridge, ipcRenderer } from 'electron'
contextBridge.exposeInMainWorld('api', {
  listNodes: () => ipcRenderer.invoke('list_nodes'),
  listInstances: () => ipcRenderer.invoke('list_instances'),
  addInstance: (name: string, port: number, node: string, password: string) =>
    ipcRenderer.invoke('add_instance', { name, port, node, password }),
  // ……26 个方法一一对应，见 ⑤.3 表格
})
```

---

### ⑤.3 `electron/ipc.ts` — 26 个 IPC handler

**逐个注册**，channel 名 = 原 Tauri command 名。**⚠️ 参数名转换**：原 Tauri 自动把前端 camelCase（`basePort`）映射到 Rust snake_case（`base_port`）；Electron 不自动映射，**在 ipc.ts 手动转换**：

```ts
ipcMain.handle('batch_add', (_e, { nodes, basePort, useNodeName, namePrefix }) =>
  addInstancesBatch(nodes, basePort ?? 18100, useNodeName ?? true, namePrefix ?? 'n'))
ipcMain.handle('scan_start', (_e, { nodes, apiPort, socksPort, timeout }) =>
  startScan(nodes ?? null, apiPort ?? 19090, socksPort ?? 29090, timeout ?? 12))
```

| channel | 参数（ipc 收到的形状） | 返回 | 服务 |
|---|---|---|---|
| `list_nodes` | – | NodeView[] | clash.ts |
| `list_instances` | – | Instance[] | instanceManager |
| `add_instance` | {name, port, node, password} | Instance | instanceManager |
| `remove_instance` | {name} | – | instanceManager |
| `start_instance` | {name} | – | instanceManager（async） |
| `stop_instance` | {name} | – | instanceManager（async） |
| `test_instance` | {name} | TestResult | instanceManager |
| `batch_add` | {nodes, basePort?, useNodeName?, namePrefix?} | BatchAddResult | instanceManager |
| `batch_start` | {names} | BatchOpResult | instanceManager |
| `batch_stop` | {names} | BatchOpResult | instanceManager |
| `batch_delete` | {names} | BatchOpResult | instanceManager |
| `port_suggest` | – | number | instanceManager |
| `port_check` | {port} | PortCheckResult | instanceManager |
| `scan_start` | {nodes?, apiPort?, socksPort?, timeout?} | ScanProgress | scanner |
| `scan_status` | – | ScanProgress | scanner |
| `scan_stop` | – | ScanProgress | scanner |
| `config_get` | – | ConfigView | configStore |
| `config_set` | {key, value} | – | configStore |
| `autostart_get` | – | boolean | main.ts（reg） |
| `autostart_set` | {enabled} | – | main.ts（reg） |
| `get_binaries_info` | – | BinariesInfo | binaries |
| （窗口三件套不进 IPC） | hide/toggleMaximize/quit | – | main.ts 直接实现 |
---

### ⑤.4 `electron/services/` — 服务层

#### ⑤.4.1 `configStore.ts` — 设置读写

**职责**：`config.json` 读写（4 个 key，格式必须与旧版一致，保证数据互通）。

```ts
// 路径：userData/config.json （userData 已由 main.ts 指向 opencode2api-manager）
export interface AppConfig {
  base_url: string; default_password: string
  clash_external_url: string; clash_auth_token: string
}
// get(key) / set(key, value)：
//   key ∈ {base_url, default_password, clash_external_url, clash_auth_token}，其他 key 报错
// set 后立即写盘（JSON pretty 2 空格）
// defaultPassword(): config.default_password 为空 → '123456'
```

#### ⑤.4.2 `http.ts` — TCP 工具（替代 Rust `http_get_json` / `wait_for_port`）

```ts
import net from 'node:net'

/** 等待 127.0.0.1:port 可连接，200ms 轮询，超时返回 false（对应 wait_for_port） */
export function waitForPort(port: number, timeoutMs: number): Promise<boolean>

/** 裸 HTTP/1.1 GET，返回 {status, body}（对应 http_get_json；chunked 由 http 层自动解） */
export function httpGetJson(port: number, path: string, timeoutMs: number): Promise<{status: number; body: string}>
// 实现：http.get(`http://127.0.0.1:${port}${path}`)，headers {Connection: close, Accept: application/json,
//       User-Agent: opencode2api-manager/0.1}
// 注意：这里是给「探活/探测」用的 —— 若启用 401 门禁，需带 Authorization: Bearer <实例密钥>（见 §七坑3）
```

#### ⑤.4.3 `clash.ts` — 节点解析

**职责**：`listNodesWithGroup()` → NodeView[]。**算法（1:1 复刻 Rust clash_yaml.rs）**：

1. **来源 A（外部控制 API，配置了 `clash_external_url` 时优先）**：
   - `GET <url>/configs`，头 `Authorization: Bearer <token>`（token 空不带）；仅支持 `http://`，https 直接报"外部控制暂不支持 https"。
   - 用 Node `http.request`（自动处理 chunked）；非 200 报错。
   - 取响应 JSON 的 `proxies` 数组 → 节点列表；每节点 `group = 节点名 split('|')[0].trim()`，无 `|` → "其他"。
2. **来源 B（本地 Clash Verge profiles，补充，按名去重）**：
   - 目录：Windows `%APPDATA%/io.github.clash-verge-rev.clash-verge-rev/profiles`。
   - 读目录下全部 `.yaml`，`js-yaml` 解析取 `proxies`；按节点名去重（A 先 B 后，重复保留 A）。
   - 分组：读同级 `profiles.yaml` 的 `items[]`（uid→name），节点组 = 订阅名；映射不到 → 文件名 stem。
3. **垃圾过滤**：名以 `-----` 开头，或含标记（登录账号/邮箱:/官网:/电报:/消息:/体验套餐:/时间:/流量重置:/剩余流量:，注意全角冒号变体 `：`）。
4. **排序**：`(group, name)` 字典序。

**ClashNode 字段**（js-yaml 直接产出，命名照抄）：`name, server, port, type, password?, uuid?, cipher?, network?, tls?, sni?, servername?, skip_cert_verify?, client_fingerprint?, ws_opts?: {path?, headers?}, group`。

**NodeView**：`{name, node_type: type, server, port, has_cred: !!password||!!uuid, group}`。

#### ⑤.4.4 `singboxCfg.ts` — sing-box 配置模板

**职责**：`buildSingboxConfig(node, listenPort): string`。**算法（1:1 复刻 singbox.rs，照抄结构）**：

```ts
// 外层
{ log: {level:'warn', timestamp:true},
  inbounds: [{type:'socks', listen:'127.0.0.1', listen_port: listenPort}],
  outbounds: [OUTBOUND, {type:'direct', tag:'direct'}],
  route: { final:'proxy' } }
// OUTBOUND 按 type：
//   trojan:  {type, tag:'proxy', server, server_port, password,
//             tls:{enabled: tls??true, server_name: sni||servername||server, insecure: skip_cert_verify??false}}
//           缺 password → throw '节点缺少 password'
//   vless:   {type, uuid, tls:{...(同 trojan，insecure 默认 false)}, transport,
//            client_fingerprint 存在时 tls.utls = {enabled:true, fingerprint}}
//           缺 uuid → throw
//   vmess:   {type, uuid, security:'auto', alter_id:0, tls:{enabled: tls??false, ...}, transport}
//           缺 uuid → throw
//   ss/shadowsocks: {type:'shadowsocks', method: cipher||'aes-256-gcm', password}
//           缺 password → throw
//   其他 → throw '暂不支持的节点类型: <type>'
// transport（vless/vmess）：network==='ws' → {type:'ws', path: ws_opts?.path||'/', headers: ws_opts?.headers||{}}
//                            ==='http' → {type:'http', host:null, path:null}
//                            缺省 → {type:'tcp'}
```

#### ⑤.4.5 `opencodeCfg.ts` — opencode2api 配置模板

**职责**：`buildOpencodeConfig(singboxPort): string`。**内容固定（照抄 opencode_cfg.rs）**：

```ts
{ model_alias: { 'deepseek-v4-flash':'deepseek-v4-flash-free', 'mimo-v2.5':'mimo-v2.5-free',
                 'ling-3.0-flash':'ling-3.0-flash-free', 'nemotron-3-ultra':'nemotron-3-ultra-free',
                 'north-mini-code':'north-mini-code-free', 'laguna-s-2.1':'laguna-s-2.1-free' },
  reasoning_effort_map: { minimal:'low', medium:'medium', high:'high' },
  force_disable_thinking: false,
  socks5_proxies: [{ name:'singbox', addr:`127.0.0.1:${singboxPort}`, username:'', password:'' }],
  active_socks5: `127.0.0.1:${singboxPort}` }
// JSON.stringify(..., null, 2)
```

#### ⑤.4.6 `binaries.ts` — 二进制释放/存在性

**职责**：启动时确保 `bin/` 下两个 exe 存在（对应 embed.rs）。

```ts
// 目标目录：path.join(path.dirname(app.getPath('exe')), 'bin')   // exe 同目录 bin/
// 源：开发期 resources/bin/；打包后 process.resourcesPath/bin（electron-builder extraResources）
// ensureFile(name)：目标不存在或 大小 ≠ 源大小 → 覆盖复制（对应 ensure_file 按 len 比较）
// getBinariesInfo(): { bin_dir, oc_exists, sb_exists }  // 判断 .exe 或裸名存在
```

#### ⑤.4.7 `instanceManager.ts` — 实例管理（最核心）

**职责**：实例 CRUD / 启停 / 探活 / 端口 / 批量。**数据结构照抄**（见 §三）。

```ts
// ── 常量
const SK_PREFIX = 'sk-'
const genSkKey = () => 'sk-' + crypto.randomBytes(8).toString('hex').slice(0, 16)
//   ↑ 格式保持 sk- + 16 位小写字母数字即可（原 LCG 算法不必复刻）

// ── 实例文件
const instancesPath = path.join(userData, 'instances.json')   // 数组，pretty JSON

// ── addInstance(name, port, node, password, ip)
//   校验：重名 → '实例 <name> 已存在'；重端口 → '端口 <port> 已被其他实例占用'
//   password 空 → genSkKey()；singbox_port = port + 10000；status 'Stopped'；写盘

// ── removeInstance(name)   运行中(Running/Starting) → '请先停止实例再删除'；否则移除写盘

// ── startInstance(name)    完整链路（照抄 §4.1，时间超时一样）：
//   1 查节点（clash.ts）→ 找不到 '未找到节点 <name>'
//   2 mkdir runtime/<name>/logs；status='Starting' 写盘
//   3 写 singbox.json；spawn sing-box.exe ['run','-c',cfg]，stdio→logs/，记 singbox_pid
//   4 await waitForPort(singbox_port, 10000)；失败 → taskkill sing-box，status=Error('sing-box 端口未就绪')
//   5 写 opencode2api.json；spawn opencode2api.exe ['-port',p,'-config',cfg,'-password',sk]，记 pid
//   6 await waitForPort(port, 15000)；失败 → taskkill 两个，status=Error('opencode2api 端口未就绪')
//   7 status='Running' 写盘

// ── stopInstance(name)     status='Stopping' → taskkill(pid) → taskkill(singbox_pid) → 'Stopped' 写盘
// ── stopAllInstances()     遍历 Running/Starting 逐个 stop（退出时调用）

// ── testInstance(name)     Running 才能测；httpGetJson(port,'/v1/models',10s) → TestResult
//   注意：启用门禁时这里要带密钥头（§七坑3）

// ── batchAdd(items, basePort=18100, useNodeName=true, prefix='n')
//   逐节点：port=item.port||base+i；name=item.name||(useNodeName? sanitize(node): `${prefix}${i+1}`)
//   sanitize：替换 / \ : * ? " < > | 与控制符 → '-'，trim、去尾'.'、空→'node'、>40 截断
//   名称冲突 → 加 '-2'..（≤100）；端口冲突 → 递增（≤200）；每实例 genSkKey()
//   返回 {added:[{name,port,node}], errors:[{node,error}], added_count, error_count}

// ── batchStart/Stop/Delete(names)  遍历执行，返回 {success[], errors{}, success_count, error_count}

// ── portSuggest()   10000~30000 区间伪随机，避开实例占用 + waitForPort(300ms)，≤200 次
// ── portCheck(p)    p<1024 throw；实例占用→{available:false,reason:'已被实例占用'}；
//                   waitForPort(300ms)→{available:false,reason:'端口已被本机程序监听'}；否则可用
```

#### ⑤.4.8 `scanner.ts` — 节点扫描控制器

**职责**：后台扫描 + 进度 + 停止（对应 probe.rs）。**算法（照抄 §4.7）**：

```ts
// 常量：DEFAULT_API_PORT=19090, DEFAULT_SOCKS_PORT=29090
// 状态：{ status:'idle'|'running'|'stopping'|'done'|'error', total, current, current_node,
//         results:ProbeResult[], error, api_port, socks_port, started_ms, finished_ms }
// startScan(nodes?, apiPort?, socksPort?, timeout?):
//   1 running 标志防重入（已跑 → throw '节点扫描已在进行中，请等待结束或先停止'）
//   2 候选节点 = clash 节点里 has_cred 的；传了 nodes 则过滤；空 → throw '没有可扫描的节点（需含 password/uuid 等凭据）'
//   3 建 runtime/_probe/logs；写探针 opencode2api.json（socks→29090）
//   4 spawn 探针 opencode2api（-port 19090 -config ... -password defaultPassword）
//     → waitForPort(19090, 15s) 失败则整体 Error
//   5 串行 for 每个节点：
//     a. current=i+1, current_node=name；检查 cancel → break
//     b. buildSingboxConfig 失败 → {ok:false, category:'config'}
//     c. 杀旧 sing-box（kill+wait+taskkill，sleep 300ms）
//     d. spawn sing-box → 失败 {category:'other'}
//     e. waitForPort(29090, min(8s, timeout)) 失败 → 读 singbox.err.log 尾 300 字符，
//        含 tls/certificate/handshake → 'tls' 否则 'socks'
//     f. sleep 400ms；httpGetJson(19090,'/v1/models', 剩余时间钳制 2~12s)
//     g. 2xx→{ok:true,'ok',model_count,latency_ms}；其他状态→{ok:false,'upstream',status_code}
//        超时/连接失败→'timeout'（可再细分为 'other'）
//     h. push result
//   6 结束：kill 探针+sing-box；finished_ms；cancel 过 → status='done' + error='扫描已中止（已完成部分结果保留）'
//     异常 → 'error'；正常 → 'done'
// requestStop(): cancel=true；status running→'stopping'
```

---

## 三、数据结构速查（TS 类型，与旧版 serde 1:1）

```ts
type InstanceStatus = 'Stopped'|'Starting'|'Running'|'Stopping'|{Error:string}
interface Instance { name:string; port:number; node:string; password:string; ip:string;
                     singbox_port:number; pid:number|null; singbox_pid:number|null; status:InstanceStatus }
interface NodeView { name:string; node_type:string; server:string; port:number; has_cred:boolean; group:string }
interface TestResult { name:string; port:number; ok:boolean; status_code:number|null;
                       model_count:number|null; message:string; latency_ms:number }
interface BatchAddItem { node:string; name?:string|null; port?:number|null }
interface BatchAddResult { added:{name:string;port:number;node:string}[]; errors:{node:string;error:string}[];
                           added_count:number; error_count:number }
interface BatchOpResult { success:string[]; errors:Record<string,string>; success_count:number; error_count:number }
interface PortCheckResult { available:boolean; reason:string }
interface ProbeResult { node:string; node_type:string; server:string; port:number; ok:boolean;
                        category:string; status_code:number|null; model_count:number|null;
                        message:string; latency_ms:number }
interface ScanProgress { status:'idle'|'running'|'stopping'|'done'|'error'; total:number; current:number;
                         current_node:string|null; results:ProbeResult[]; error:string|null;
                         api_port:number; socks_port:number; started_ms:number|null; finished_ms:number|null }
interface ConfigView { base_url:string; default_password:string; has_password:boolean;
                       clash_external_url:string; has_clash_token:boolean }
interface BinariesInfo { bin_dir:string; oc_exists:boolean; sb_exists:boolean }
```
---

## 三、前端迁移（`src/` 节点）

**做法**：把 `opencode2api/src/` 整体复制进 `src/`，**只改 1 个文件**（`lib/api.ts`），其余页面/组件原样保留。

### ⑥.1 `components/TitleBar.tsx` — 唯一要改的组件

| 原（Tauri） | 改（Electron） |
|---|---|
| `data-tauri-drag-region` 拖拽 | `style={{WebkitAppRegion:'drag'}}`；按钮容器 `no-drag` |
| `getCurrentWindow().minimize()` | `window.api.minimize()`（preload → ipcMain → win.minimize） |
| `getCurrentWindow().toggleMaximize()` | `window.api.toggleMaximize()`；最大化状态由主进程 `win.on('maximize')` 推送 |
| `getCurrentWindow().hide()`（关闭） | `window.api.hideWindow()` |
| 双击标题栏最大化 | 保留 onDoubleClick → toggleMaximize |

### ⑥.2 `pages/` — 三个页面原样复制

交互细节已全部记录，实现时逐项对照（**全部行为都要有**）：

- **InstancesPage**：工具条（刷新/添加/批量启动/批量停止/批量删除）、表格 6 列（勾选|名称/节点IP|端口|API地址|密钥|状态|操作）、状态徽章配色（Running 绿/Stopped 灰/Starting·Stopping 琥珀/Error 红）、启动/停止按状态切换、测试 toast（通过含延迟）、删除 confirm、添加弹窗（名称留空自动/节点必填/端口≥1024）、**3s setInterval 轮询**、密钥脱敏 `sk-abc…wxyz`、空态提示。
- **NodesPage**：分组折叠列表（组全选、已选计数）、**已添加实例节点禁用勾选 + 绿标"✓ 已添加实例"**、凭据标记（✓凭据/✗无凭据）、扫描进度条（当前/总数·当前节点 + 百分比，**停止即隐藏**）、结果徽章（ok绿/upstream琥珀/config·socks·tls·timeout红/other灰 + 延迟ms）、失败 message 截断 hover 全显、**800ms setTimeout 链式轮询**、一键"添加选中为实例"（batchAdd 18100/true）。
- **SettingsPage**：Clash 外部控制（URL + 密钥 password 输入，token 留空不修改，保存 toast）、开机自启 Toggle（Windows 注册表说明）、关于（二进制目录 mono + 两个 exe ✓/✗ + 内嵌说明）、`max-w-2xl mx-auto` 布局。

### ⑥.3 `lib/api.ts` — 唯一要改的数据层

- 全部 `invoke<T>('xxx', args)` 换成 `window.api.xxx(args)`。
- **类型定义原样保留**（§三 速查表已列出，直接照抄现文件）。
- 注意现文件里 `invoke('add_instance', {name, port, node, password})` 传的是 snake_case；preload 里参数名保持一致即可（ipc.ts 里再处理默认值）。

---

## 五、`resources/` 与 `README.txt`

### ⑦ `resources/bin/`

从旧机器复制：`opencode2api/bin/opencode2api.exe` + `sing-box.exe`（这是唯一的外部二进制来源）。
- 若要启用 401 门禁：在新机器 `cd opencode2api && go build -trimpath -ldflags "-s -w" -o ../opencode2api_electron/resources/bin/opencode2api.exe .` 后再放进来。

### ⑧ `README.txt`

从 `opencode2api/portable/README.txt` 复制（便携包使用说明）。

---

## 五、构建与交付

```bash
# 1. 前端 + 主进程
npm install
npm run build && npm run build:electron
# 2. 打包（便携版 + 安装版）
npx electron-builder --win portable,nsis
# 产物在 dist/：opencode2api管理器-0.1.0-portable.exe 等
```

**交付验收**：双击便携 exe → 托盘出现 → 设置页 Clash 外部控制 → 节点页扫描 → 添加实例 → 启动 → 复制 API 地址+密钥给 opencode 客户端 → 请求成功。

---

## 六、功能验证清单（交付前逐项勾选）

- [ ] 节点：外部控制优先 / 本地 Verge 补充 / 垃圾过滤 / 分组排序 / 已添加节点灰显
- [ ] 实例：添加（重名/重端口拒绝、端口<1024 拒绝、自动 sk- 密钥、singbox_port=port+10000）
- [ ] 启动：sing-box→SOCKS 就绪→opencode2api→API 就绪→Running；日志落盘
- [ ] 停止：两个进程都被 taskkill；状态 Stopped
- [ ] 运行中实例禁止删除；删除有 confirm
- [ ] 测试：GET /v1/models 返回模型数+延迟；失败给原因
- [ ] 批量：添加/启动/停止/删除，成功失败计数正确，名称冲突 -2 后缀、端口冲突递增
- [ ] 实例列表 3s 自动刷新；操作后 toast
- [ ] 扫描：进度条（当前/总数/百分比/当前节点）、停止保留部分结果、分类徽章+延迟、防重入、一键添加
- [ ] 设置：Clash 外部控制保存、token 留空不修改、开机自启 toggle、关于页二进制状态
- [ ] 窗口/托盘：关窗隐藏（实例继续跑）、托盘显示/退出、退出前停全部实例
- [ ] 数据兼容：旧机器 instances.json/config.json 复制到 userData 后原样加载
- [ ] 代理回归：/v1/models、/v1/chat/completions（流式+非流式）、/v1/messages（Anthropic）
- [ ] （启用门禁时）无 key 401、正确 key 200、go:/zen: 前缀可用

---

## 七、已知坑（实现前必读，按重要性排序）

1. **🔴 探活不带密钥 vs 401 门禁冲突**：`testInstance` 和 `scanner` 的 `GET /v1/models` 原实现**不带 Authorization**。若启用门禁，这两处必须带 `Bearer <实例密钥>`（或门禁对 /v1/models 放行）。**二选一，实现时决定并统一**。
2. **🔴 userData 路径**：必须 `app.setPath('userData', ...)` 指向 `opencode2api-manager`，否则旧数据不可见、实例列表空。
3. **🔴 IPC 参数名**：Tauri 自动 camelCase↔snake_case，Electron 不自动，ipc.ts 手动转换（已示范）。
4. **🟡 监听 0.0.0.0**：Go 代理 `addr = ":" + port`，局域网可访问——门禁是唯一防线。
5. **🟡 Go 源码母本**：以 `opencode2api/main.go` 为准（含 401 改动）；`opencode2api_enhance/main.go` 是旧版，不要拿来编译。
6. **🟡 taskkill /F**：Windows 下 `child.kill()` 可能杀不干净，保持 `taskkill /PID <pid> /F`。
7. **🟡 sing-box 只绑 127.0.0.1**：socks 监听写死 127.0.0.1；opencode2api 配置的 active_socks5 指它，两个配置一对，别拆散。
8. **🟡 非 Windows**：开机自启与 Verge profiles 解析仅 Windows（原版行为）；Electron 可顺手补 macOS/Linux，属增强。
9. **🟢 前端轮询节奏**：实例页 3s setInterval、扫描页 800ms setTimeout 链——保持原样。
10. **🟢 无边框窗口**：Linux 下 frame:false 需验证；异常时回退原生边框（行为差异可接受）。

---

## 附录 A：窗口/托盘/生命周期对照表

| 行为 | Tauri 实现 | Electron 实现 |
|---|---|---|
| 窗口 980×680 min820×560 居中 无边框 | tauri.conf.json | BrowserWindow 参数（⑤.1） |
| 自绘标题栏拖拽/双击最大化 | data-tauri-drag-region | WebkitAppRegion drag（⑥.1） |
| 关闭 = 隐藏（实例继续跑） | on_window_event CloseRequested → prevent_close + hide | win.on('close') → preventDefault + hide（⑤.1） |
| 托盘菜单：显示主窗口/退出 | MenuItem × 2 | Tray context menu × 2（⑤.1） |
| 托盘左键单击显示 | on_tray_icon_event Click | tray.on('click')（⑤.1） |
| 退出 = 停全部实例 | RunEvent::ExitRequested → stop_all_instances | before-quit → await stopAllInstances → app.exit（⑤.1） |
| 开机自启（Windows 注册表） | reg query/add/delete | reg 命令照旧，或 app.setLoginItemSettings（⑤.3/§5.4） |
| 最小化/最大化/关闭按钮 | tauri command | preload + ipcMain → win API（⑥.1） |

## 附录 B：Go 代理（opencode2api.exe）功能速查

> Electron 只负责拉起/停止它，功能本身在 Go 侧。若未来用 Node 重写代理，需覆盖以下全部：

- **协议**：`POST /v1/chat/completions`（含 stream）、`POST /v1/responses`、`POST /v1/messages`（Anthropic）、`GET /v1/models`。
- **上游路由**：无头/占位 key → public（`Bearer public` 免费通道）；`sk-` key → auto；`go:`/`zen:` 前缀强制对应 surface；转发 `https://opencode.ai/zen/{go/}v1/...`。
- **模型目录**：免费目录 + go 目录启动拉取、定时刷新，按认证档位过滤；`getCandidateModels` 同端点回退避免跨目录 401。
- **配置项**：model_alias / reasoning_effort_map / force_disable_thinking / socks5_proxies / active_socks5（管理器生成）。
- **管理面板**：/login /logout /api/config /api/stats /api/reload /health；密码 = -password；session cookie。
- **API 门禁（已改未编译）**：`apiKeyAuthMiddleware` — /v1/* 必须带 `Bearer <adminPassword>`（支持 go:/zen: 前缀）否则 401 JSON；adminPassword 空放行。
